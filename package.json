{
  "name": "multy-game",
  "version": "1.0.0",
  "type": "module",
  "homepage": "https://zarantuza.github.io/multy-game/",
  "scripts": { 
   "dev": "vite",
    "build": "vite build",
    "deploy": "vite build && powershell -Command \"Copy-Item dist/* . -Recurse -Force; Remove-Item dist -Recurse -Force; New-Item -ItemType File -Path .nojekyll -Force\""
  },
  "dependencies": {
    "simple-peer": "^9.11.1"
  },
  "devDependencies": {
    "vite": "^4.3.9"
  }
}